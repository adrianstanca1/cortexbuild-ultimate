// CortexBuild Ultimate — legacy barrel (replaces Supabase); use auth-storage for new code
export {
  getToken,
  setToken,
  clearToken,
  getStoredUser,
  setStoredUser,
  API_BASE,
} from './auth-storage';
