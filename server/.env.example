DB_HOST=localhost
DB_PORT=5432
DB_NAME=cortexbuild
DB_USER=cortexbuild
DB_PASSWORD=CortexBuild2024!
JWT_SECRET=cortexbuild_jwt_secret_change_this_in_production
PORT=3001

# ─── AI / Ollama (optional — for local LLM features) ──────────────────────────
# Run scripts/setup-ollama.sh to install Ollama and pull a model
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2:3b
# Options by RAM: 32GB+ → qwen2.5:14b | 16GB+ → qwen2.5:7b | 8GB → llama3.2:3b

# ─── External AI (optional — alternative to Ollama) ──────────────────────────
# GEMINI_API_KEY=your_gemini_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
